from DjangoFlot.plot.data.datas import PlotData
from DjangoFlot.plot.opt.options import PlotOptions

class PlotView(object):
    """
    This class represents a plot, it has the data and the options.
    """

    def __init__(self):
    
        self.datas = PlotData[]
        self.options = PlotOptions
    
    
#    def __unicode__(self):
#        return force_unicode(self.label)

    def printPlotData(self):
        #serialize DATA to JSON.
        return finalString
           
